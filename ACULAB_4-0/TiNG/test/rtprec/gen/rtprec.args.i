/* Automatically generated by mkargs - DO NOT EDIT! */
/* Built by command: ../../libutil/mkargs.pl -v ../../spTiNG-v -r ../../relver rtprec.args */

struct arguments {
	unsigned sync;
	char *identifier;
	signed volume;
	unsigned enforce_tone_spacing;
	unsigned rfc2833tones;
	unsigned max_silence;
	unsigned sample_rate;
	unsigned numchan;
	unsigned payloadmapping;
	unsigned max_elapsed_time;
	unsigned initial_ms;
	unsigned regen_tones;
	unsigned sil_elim;
	unsigned detect_tones;
	char *codec;
	unsigned agc;
	unsigned maxoctets;
	unsigned underrun_flag;
	unsigned threshold;
	unsigned sse_payload_type;
	unsigned tone_set;
	unsigned per_mod;
	char *local_ip;
	char *srtp_key;
	unsigned report_jb_resync;
	unsigned max_ms;
	char *format;
	unsigned tone_elim_mode;
	unsigned use_srtp;
	unsigned clerk_port;
	unsigned bitrate;
	char *clerk_addr;
	unsigned variant;
	unsigned plc;
	unsigned comfort_noise_payload_mapping;
	unsigned comfort_noise;
	unsigned frame_len;
	float ad_freq_upper_tolerance;
	unsigned ad_target_delay;
	float ad_freq_lower_tolerance;
	unsigned vmp_is_ipv6;
};

#define ARGS_DECL static struct arguments arg;
#define ARGS_CALL (progname = *argv++),args(&arg, &argv)

#define ARGS_USAGE " [--version] [-y =sync] [-x identifier] [-v volume] [-s enforce_tone_spacing] [-r rfc2833tones] [-q max_silence] [-oR sample_rate] [-n numchan] [-m payloadmapping] [-l max_elapsed_time] [-j initial_ms] [-g =regen_tones] [-e sil_elim] [-d =detect_tones] [-c codec] [-a agc] [-X maxoctets] [-U =underrun_flag] [-T threshold] [-SSEp sse_payload_type] [-S tone_set] [-P per_mod] [-L local_ip] [-K srtp_key] [-Jr =report_jb_resync] [-J max_ms] [-F format] [-E tone_elim_mode] [-Cs =use_srtp] [-Cp clerk_port] [-Cb bitrate] [-Ca clerk_addr] [-CV variant] [-CP =plc] [-CNm comfort_noise_payload_mapping] [-CNe =comfort_noise] [-CF frame_len] [-Au ad_freq_upper_tolerance] [-At ad_target_delay] [-Al ad_freq_lower_tolerance] [-6 =vmp_is_ipv6]"

char *progname;

static const char version[] = "@(#)$from: " __FILE__ " Aculab Telephony Software V2.53.7.m1504. $";

#include <string.h>

enum args_result {
	ARGS_NOMATCH,
	ARGS_OK,
	ARGS_BAD_NOMSG,
	ARGS_BAD_NOARG,
};

static char *args_prefix(const char *pfx, char *s)
{
	for (;;) {
		if (!*pfx) return s;
		if (*pfx++ != *s++) return 0;
	}
}

static char *args_try_bool(struct arguments *ap, char *sp)
{
	for (; *sp;) {
		char *ep;
		if (*sp == 'y') ep = sp+1, ap->sync = 1;
		else if (*sp == 'g') ep = sp+1, ap->regen_tones = 1;
		else if (*sp == 'd') ep = sp+1, ap->detect_tones = 1;
		else if (*sp == 'U') ep = sp+1, ap->underrun_flag = 1;
		else if ((ep = args_prefix("Jr", sp))) ap->report_jb_resync = 1;
		else if ((ep = args_prefix("Cs", sp))) ap->use_srtp = 1;
		else if ((ep = args_prefix("CP", sp))) ap->plc = 1;
		else if ((ep = args_prefix("CNe", sp))) ap->comfort_noise = 1;
		else if (*sp == '6') ep = sp+1, ap->vmp_is_ipv6 = 1;
		else break;
		sp = ep;
	}
	return sp;
}

#include <stdlib.h>

static enum args_result args_try_unsigned(struct arguments *ap, char *sp, char ***argv)
{
	unsigned *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("-TiNGtrace", sp))) {
		t = &TiNGtrace;
	} else if (*sp == 's') ep = sp+1, t = &ap->enforce_tone_spacing;
	else if (*sp == 'r') ep = sp+1, t = &ap->rfc2833tones;
	else if (*sp == 'q') ep = sp+1, t = &ap->max_silence;
	else if ((ep = args_prefix("oR", sp))) t = &ap->sample_rate;
	else if (*sp == 'n') ep = sp+1, t = &ap->numchan;
	else if (*sp == 'm') ep = sp+1, t = &ap->payloadmapping;
	else if (*sp == 'l') ep = sp+1, t = &ap->max_elapsed_time;
	else if (*sp == 'j') ep = sp+1, t = &ap->initial_ms;
	else if (*sp == 'e') ep = sp+1, t = &ap->sil_elim;
	else if (*sp == 'a') ep = sp+1, t = &ap->agc;
	else if (*sp == 'X') ep = sp+1, t = &ap->maxoctets;
	else if (*sp == 'T') ep = sp+1, t = &ap->threshold;
	else if ((ep = args_prefix("SSEp", sp))) t = &ap->sse_payload_type;
	else if (*sp == 'S') ep = sp+1, t = &ap->tone_set;
	else if (*sp == 'P') ep = sp+1, t = &ap->per_mod;
	else if (*sp == 'J') ep = sp+1, t = &ap->max_ms;
	else if (*sp == 'E') ep = sp+1, t = &ap->tone_elim_mode;
	else if ((ep = args_prefix("Cp", sp))) t = &ap->clerk_port;
	else if ((ep = args_prefix("Cb", sp))) t = &ap->bitrate;
	else if ((ep = args_prefix("CV", sp))) t = &ap->variant;
	else if ((ep = args_prefix("CNm", sp))) t = &ap->comfort_noise_payload_mapping;
	else if ((ep = args_prefix("CF", sp))) t = &ap->frame_len;
	else if ((ep = args_prefix("At", sp))) t = &ap->ad_target_delay;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtoul(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_signed(struct arguments *ap, char *sp, char ***argv)
{
	signed *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if (*sp == 'v') ep = sp+1, t = &ap->volume;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtol(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_float(struct arguments *ap, char *sp, char ***argv)
{
	float *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("Au", sp))) t = &ap->ad_freq_upper_tolerance;
	else if ((ep = args_prefix("Al", sp))) t = &ap->ad_freq_lower_tolerance;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtod(tp, &ep);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

static enum args_result args_try_string(struct arguments *ap, char *sp, char ***argv)
{
	char *ep, **t;
	if (*sp == 'x') ep = sp+1, t = &ap->identifier;
	else if (*sp == 'c') ep = sp+1, t = &ap->codec;
	else if (*sp == 'L') ep = sp+1, t = &ap->local_ip;
	else if (*sp == 'K') ep = sp+1, t = &ap->srtp_key;
	else if (*sp == 'F') ep = sp+1, t = &ap->format;
	else if ((ep = args_prefix("Ca", sp))) t = &ap->clerk_addr;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	*t = ep;
	return ARGS_OK;
}

static int args(struct arguments *ap, char ***argv)
{
	for (; **argv; ++*argv) {
		char *ep = **argv;
		if (*ep++ != '-' || !*ep) break;
		if (0) {
		} else if (!strcmp(ep, "-version")) {
			fprintf(stderr, "%s: version %s\n", progname, version);
		} else {
			int i;
			ep = args_try_bool(ap, ep);
			if (!*ep) continue;
			if (!strcmp(ep, "-")) {
				++*argv;
				return 0;
			}
			if (1
				 && !(i = args_try_unsigned(ap, ep, argv))
				 && !(i = args_try_signed(ap, ep, argv))
				 && !(i = args_try_float(ap, ep, argv))
				 && !(i = args_try_string(ap, ep, argv))
			) {
				fprintf(stderr, "unrecognised option: %s\n", **argv);
				return 1;
			} else switch (i) {
				case ARGS_OK: break;
				case ARGS_BAD_NOMSG: return 1;
				case ARGS_BAD_NOARG:
				fprintf(stderr, "value missing for '%s'\n", **argv);
				return 1;
			}
		}
	}
	return 0;
}
