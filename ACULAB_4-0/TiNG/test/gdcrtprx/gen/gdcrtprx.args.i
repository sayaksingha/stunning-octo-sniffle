/* Automatically generated by mkargs - DO NOT EDIT! */
/* Built by command: ../../libutil/mkargs.pl -v ../../spTiNG-v -r ../../relver gdcrtprx.args */

struct arguments {
	char *serialnumber;
	char *modulation;
	char *standard;
	unsigned patlen;
	char *outfile;
	unsigned module;
	unsigned min_flags;
	char *format;
	char *encoding;
	unsigned databits;
	unsigned crc;
	unsigned useevent;
	unsigned xfertimeout;
	signed xferbits;
	float spacefreq;
	unsigned enforce_tone_spacing;
	unsigned rfc2833tones;
	unsigned payloadmapping;
	unsigned jitter_initial_ms;
	unsigned regen_tones;
	unsigned detect_tones;
	char *codec;
	char *local_ip;
	unsigned jitter_max_ms;
	unsigned use_srtp;
	unsigned codec_bitrate;
	unsigned variant;
	unsigned plc;
	unsigned comfort_noise_payload_mapping;
	unsigned comfort_noise;
	unsigned frame_len;
	float ad_freq_upper_tolerance;
	unsigned ad_target_delay;
	float ad_freq_lower_tolerance;
	unsigned use_ipv6;
	unsigned min_ones;
	float markfreq;
	char *source_ip;
	unsigned source_port;
	unsigned dest_port;
	char *dest_ip;
	unsigned carrier_on;
	unsigned carrier_off;
	unsigned crcsize;
	unsigned bitrate;
	unsigned answer;
};

#define ARGS_DECL static struct arguments arg;
#define ARGS_CALL (progname = *argv++),args(&arg, &argv)

#define ARGS_USAGE " [--version] [-x serialnumber] [-u modulation] [-s standard] [-p patlen] [-o outfile] [-m module] [-l min_flags] [-f format] [-e encoding] [-d databits] [-c crc] [-U =useevent] [-Tt xfertimeout] [-Tb xferbits] [-S spacefreq] [-Rs enforce_tone_spacing] [-Rr rfc2833tones] [-Rm payloadmapping] [-Rj jitter_initial_ms] [-Rg =regen_tones] [-Rd =detect_tones] [-Rc codec] [-RL local_ip] [-RJ jitter_max_ms] [-RE =use_srtp] [-RCb codec_bitrate] [-RCV variant] [-RCP =plc] [-RCNm comfort_noise_payload_mapping] [-RCNe =comfort_noise] [-RCF frame_len] [-RAu ad_freq_upper_tolerance] [-RAt ad_target_delay] [-RAl ad_freq_lower_tolerance] [-R6 =use_ipv6] [-O min_ones] [-M markfreq] [-Is source_ip] [-Iq source_port] [-Ip dest_port] [-Id dest_ip] [-Fon carrier_on] [-Foff carrier_off] [-C crcsize] [-B bitrate] [-Ans =answer]"

char *progname;

static const char version[] = "@(#)$from: " __FILE__ " Aculab Telephony Software V2.53.7.m1504. $";

#include <string.h>

enum args_result {
	ARGS_NOMATCH,
	ARGS_OK,
	ARGS_BAD_NOMSG,
	ARGS_BAD_NOARG,
};

static char *args_prefix(const char *pfx, char *s)
{
	for (;;) {
		if (!*pfx) return s;
		if (*pfx++ != *s++) return 0;
	}
}

static char *args_try_bool(struct arguments *ap, char *sp)
{
	for (; *sp;) {
		char *ep;
		if (*sp == 'U') ep = sp+1, ap->useevent = 1;
		else if ((ep = args_prefix("Rg", sp))) ap->regen_tones = 1;
		else if ((ep = args_prefix("Rd", sp))) ap->detect_tones = 1;
		else if ((ep = args_prefix("RE", sp))) ap->use_srtp = 1;
		else if ((ep = args_prefix("RCP", sp))) ap->plc = 1;
		else if ((ep = args_prefix("RCNe", sp))) ap->comfort_noise = 1;
		else if ((ep = args_prefix("R6", sp))) ap->use_ipv6 = 1;
		else if ((ep = args_prefix("Ans", sp))) ap->answer = 1;
		else break;
		sp = ep;
	}
	return sp;
}

#include <stdlib.h>

static enum args_result args_try_unsigned(struct arguments *ap, char *sp, char ***argv)
{
	unsigned *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("-TiNGtrace", sp))) {
		t = &TiNGtrace;
	} else if (*sp == 'p') ep = sp+1, t = &ap->patlen;
	else if (*sp == 'm') ep = sp+1, t = &ap->module;
	else if (*sp == 'l') ep = sp+1, t = &ap->min_flags;
	else if (*sp == 'd') ep = sp+1, t = &ap->databits;
	else if (*sp == 'c') ep = sp+1, t = &ap->crc;
	else if ((ep = args_prefix("Tt", sp))) t = &ap->xfertimeout;
	else if ((ep = args_prefix("Rs", sp))) t = &ap->enforce_tone_spacing;
	else if ((ep = args_prefix("Rr", sp))) t = &ap->rfc2833tones;
	else if ((ep = args_prefix("Rm", sp))) t = &ap->payloadmapping;
	else if ((ep = args_prefix("Rj", sp))) t = &ap->jitter_initial_ms;
	else if ((ep = args_prefix("RJ", sp))) t = &ap->jitter_max_ms;
	else if ((ep = args_prefix("RCb", sp))) t = &ap->codec_bitrate;
	else if ((ep = args_prefix("RCV", sp))) t = &ap->variant;
	else if ((ep = args_prefix("RCNm", sp))) t = &ap->comfort_noise_payload_mapping;
	else if ((ep = args_prefix("RCF", sp))) t = &ap->frame_len;
	else if ((ep = args_prefix("RAt", sp))) t = &ap->ad_target_delay;
	else if (*sp == 'O') ep = sp+1, t = &ap->min_ones;
	else if ((ep = args_prefix("Iq", sp))) t = &ap->source_port;
	else if ((ep = args_prefix("Ip", sp))) t = &ap->dest_port;
	else if ((ep = args_prefix("Fon", sp))) t = &ap->carrier_on;
	else if ((ep = args_prefix("Foff", sp))) t = &ap->carrier_off;
	else if (*sp == 'C') ep = sp+1, t = &ap->crcsize;
	else if (*sp == 'B') ep = sp+1, t = &ap->bitrate;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtoul(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_signed(struct arguments *ap, char *sp, char ***argv)
{
	signed *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("Tb", sp))) t = &ap->xferbits;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtol(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_float(struct arguments *ap, char *sp, char ***argv)
{
	float *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if (*sp == 'S') ep = sp+1, t = &ap->spacefreq;
	else if ((ep = args_prefix("RAu", sp))) t = &ap->ad_freq_upper_tolerance;
	else if ((ep = args_prefix("RAl", sp))) t = &ap->ad_freq_lower_tolerance;
	else if (*sp == 'M') ep = sp+1, t = &ap->markfreq;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtod(tp, &ep);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

static enum args_result args_try_string(struct arguments *ap, char *sp, char ***argv)
{
	char *ep, **t;
	if (*sp == 'x') ep = sp+1, t = &ap->serialnumber;
	else if (*sp == 'u') ep = sp+1, t = &ap->modulation;
	else if (*sp == 's') ep = sp+1, t = &ap->standard;
	else if (*sp == 'o') ep = sp+1, t = &ap->outfile;
	else if (*sp == 'f') ep = sp+1, t = &ap->format;
	else if (*sp == 'e') ep = sp+1, t = &ap->encoding;
	else if ((ep = args_prefix("Rc", sp))) t = &ap->codec;
	else if ((ep = args_prefix("RL", sp))) t = &ap->local_ip;
	else if ((ep = args_prefix("Is", sp))) t = &ap->source_ip;
	else if ((ep = args_prefix("Id", sp))) t = &ap->dest_ip;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	*t = ep;
	return ARGS_OK;
}

static int args(struct arguments *ap, char ***argv)
{
	for (; **argv; ++*argv) {
		char *ep = **argv;
		if (*ep++ != '-' || !*ep) break;
		if (0) {
		} else if (!strcmp(ep, "-version")) {
			fprintf(stderr, "%s: version %s\n", progname, version);
		} else {
			int i;
			ep = args_try_bool(ap, ep);
			if (!*ep) continue;
			if (!strcmp(ep, "-")) {
				++*argv;
				return 0;
			}
			if (1
				 && !(i = args_try_unsigned(ap, ep, argv))
				 && !(i = args_try_signed(ap, ep, argv))
				 && !(i = args_try_float(ap, ep, argv))
				 && !(i = args_try_string(ap, ep, argv))
			) {
				fprintf(stderr, "unrecognised option: %s\n", **argv);
				return 1;
			} else switch (i) {
				case ARGS_OK: break;
				case ARGS_BAD_NOMSG: return 1;
				case ARGS_BAD_NOARG:
				fprintf(stderr, "value missing for '%s'\n", **argv);
				return 1;
			}
		}
	}
	return 0;
}
