/* Automatically generated by mkargs - DO NOT EDIT! */
/* Built by command: ../../libutil/mkargs.pl -v ../../spTiNG-v -r ../../relver gdcrtptx.args */

struct arguments {
	char *serialnumber;
	char *modulation;
	char *standard;
	unsigned patlen;
	unsigned module;
	char *inifile;
	char *encoding;
	unsigned databits;
	unsigned crc;
	unsigned stopbits;
	float spacefreq;
	unsigned source_port;
	unsigned dest_port;
	unsigned payloadmapping;
	unsigned rfc2833tones;
	char *dest_ip;
	char *codec;
	char *srtp_key;
	unsigned vad;
	unsigned pticks;
	unsigned codec_bitrate;
	unsigned variant;
	unsigned comfort_noise_payload_mapping;
	unsigned frame_len;
	unsigned high_pass_filter;
	float power;
	float markfreq;
	char *local_ip;
	unsigned crcsize;
	unsigned bitrate;
};

#define ARGS_DECL static struct arguments arg;
#define ARGS_CALL (progname = *argv++),args(&arg, &argv)

#define ARGS_USAGE " [--version] [-x serialnumber] [-u modulation] [-s standard] [-p patlen] [-m module] [-i inifile] [-e encoding] [-d databits] [-c crc] [-X stopbits] [-S spacefreq] [-Rq source_port] [-Rp dest_port] [-Rm payloadmapping] [-Rf rfc2833tones] [-Rd dest_ip] [-Rc codec] [-RK srtp_key] [-RCv vad] [-RCt pticks] [-RCb codec_bitrate] [-RCV variant] [-RCNm comfort_noise_payload_mapping] [-RCL frame_len] [-RCH =high_pass_filter] [-P power] [-M markfreq] [-L local_ip] [-C crcsize] [-B bitrate]"

char *progname;

static const char version[] = "@(#)$from: " __FILE__ " Aculab Telephony Software V2.53.7.m1504. $";

#include <string.h>

enum args_result {
	ARGS_NOMATCH,
	ARGS_OK,
	ARGS_BAD_NOMSG,
	ARGS_BAD_NOARG,
};

static char *args_prefix(const char *pfx, char *s)
{
	for (;;) {
		if (!*pfx) return s;
		if (*pfx++ != *s++) return 0;
	}
}

static char *args_try_bool(struct arguments *ap, char *sp)
{
	for (; *sp;) {
		char *ep;
		if ((ep = args_prefix("RCH", sp))) ap->high_pass_filter = 1;
		else break;
		sp = ep;
	}
	return sp;
}

#include <stdlib.h>

static enum args_result args_try_unsigned(struct arguments *ap, char *sp, char ***argv)
{
	unsigned *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("-TiNGtrace", sp))) {
		t = &TiNGtrace;
	} else if (*sp == 'p') ep = sp+1, t = &ap->patlen;
	else if (*sp == 'm') ep = sp+1, t = &ap->module;
	else if (*sp == 'd') ep = sp+1, t = &ap->databits;
	else if (*sp == 'c') ep = sp+1, t = &ap->crc;
	else if (*sp == 'X') ep = sp+1, t = &ap->stopbits;
	else if ((ep = args_prefix("Rq", sp))) t = &ap->source_port;
	else if ((ep = args_prefix("Rp", sp))) t = &ap->dest_port;
	else if ((ep = args_prefix("Rm", sp))) t = &ap->payloadmapping;
	else if ((ep = args_prefix("Rf", sp))) t = &ap->rfc2833tones;
	else if ((ep = args_prefix("RCv", sp))) t = &ap->vad;
	else if ((ep = args_prefix("RCt", sp))) t = &ap->pticks;
	else if ((ep = args_prefix("RCb", sp))) t = &ap->codec_bitrate;
	else if ((ep = args_prefix("RCV", sp))) t = &ap->variant;
	else if ((ep = args_prefix("RCNm", sp))) t = &ap->comfort_noise_payload_mapping;
	else if ((ep = args_prefix("RCL", sp))) t = &ap->frame_len;
	else if (*sp == 'C') ep = sp+1, t = &ap->crcsize;
	else if (*sp == 'B') ep = sp+1, t = &ap->bitrate;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtoul(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_float(struct arguments *ap, char *sp, char ***argv)
{
	float *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if (*sp == 'S') ep = sp+1, t = &ap->spacefreq;
	else if (*sp == 'P') ep = sp+1, t = &ap->power;
	else if (*sp == 'M') ep = sp+1, t = &ap->markfreq;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtod(tp, &ep);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

static enum args_result args_try_string(struct arguments *ap, char *sp, char ***argv)
{
	char *ep, **t;
	if (*sp == 'x') ep = sp+1, t = &ap->serialnumber;
	else if (*sp == 'u') ep = sp+1, t = &ap->modulation;
	else if (*sp == 's') ep = sp+1, t = &ap->standard;
	else if (*sp == 'i') ep = sp+1, t = &ap->inifile;
	else if (*sp == 'e') ep = sp+1, t = &ap->encoding;
	else if ((ep = args_prefix("Rd", sp))) t = &ap->dest_ip;
	else if ((ep = args_prefix("Rc", sp))) t = &ap->codec;
	else if ((ep = args_prefix("RK", sp))) t = &ap->srtp_key;
	else if (*sp == 'L') ep = sp+1, t = &ap->local_ip;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	*t = ep;
	return ARGS_OK;
}

static int args(struct arguments *ap, char ***argv)
{
	for (; **argv; ++*argv) {
		char *ep = **argv;
		if (*ep++ != '-' || !*ep) break;
		if (0) {
		} else if (!strcmp(ep, "-version")) {
			fprintf(stderr, "%s: version %s\n", progname, version);
		} else {
			int i;
			ep = args_try_bool(ap, ep);
			if (!*ep) continue;
			if (!strcmp(ep, "-")) {
				++*argv;
				return 0;
			}
			if (1
				 && !(i = args_try_unsigned(ap, ep, argv))
				 && !(i = args_try_float(ap, ep, argv))
				 && !(i = args_try_string(ap, ep, argv))
			) {
				fprintf(stderr, "unrecognised option: %s\n", **argv);
				return 1;
			} else switch (i) {
				case ARGS_OK: break;
				case ARGS_BAD_NOMSG: return 1;
				case ARGS_BAD_NOARG:
				fprintf(stderr, "value missing for '%s'\n", **argv);
				return 1;
			}
		}
	}
	return 0;
}
