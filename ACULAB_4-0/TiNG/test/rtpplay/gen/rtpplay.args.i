/* Automatically generated by mkargs - DO NOT EDIT! */
/* Built by command: ../../libutil/mkargs.pl -v ../../spTiNG-v -r ../../relver rtpplay.args */

struct arguments {
	unsigned sync;
	char *identifier;
	signed volume;
	signed speed;
	unsigned restart;
	unsigned source_port;
	unsigned dest_port;
	unsigned out_sample_rate;
	unsigned numchan;
	unsigned payloadmapping;
	unsigned rfc2833tones;
	char *src_ip;
	char *dest_ip;
	char *codec;
	unsigned agc;
	unsigned maxoctets;
	signed threshold;
	unsigned sse_repeat_spacing;
	unsigned sse_repeat_count;
	unsigned sse_payload_type;
	unsigned randdelay;
	unsigned per_mod;
	unsigned once;
	char *srtp_key;
	char *format;
	unsigned vad;
	unsigned pticks;
	unsigned use_srtp;
	unsigned clerk_port;
	unsigned bitrate;
	char *clerk_addr;
	unsigned variant;
	unsigned comfort_noise_payload_mapping;
	unsigned frame_len;
	unsigned high_pass_filter;
};

#define ARGS_DECL static struct arguments arg;
#define ARGS_CALL (progname = *argv++),args(&arg, &argv)

#define ARGS_USAGE " [--version] [-y =sync] [-x identifier] [-v volume] [-s speed] [-r =restart] [-q source_port] [-p dest_port] [-oR out_sample_rate] [-n numchan] [-m payloadmapping] [-f rfc2833tones] [-e src_ip] [-d dest_ip] [-c codec] [-a agc] [-X maxoctets] [-T threshold] [-SSEt sse_repeat_spacing] [-SSEr sse_repeat_count] [-SSEp sse_payload_type] [-R randdelay] [-P per_mod] [-O =once] [-K srtp_key] [-F format] [-Cv vad] [-Ct pticks] [-Cs =use_srtp] [-Cp clerk_port] [-Cb bitrate] [-Ca clerk_addr] [-CV variant] [-CNm comfort_noise_payload_mapping] [-CL frame_len] [-CH =high_pass_filter]"

char *progname;

static const char version[] = "@(#)$from: " __FILE__ " Aculab Telephony Software V2.53.7.m1504. $";

#include <string.h>

enum args_result {
	ARGS_NOMATCH,
	ARGS_OK,
	ARGS_BAD_NOMSG,
	ARGS_BAD_NOARG,
};

static char *args_prefix(const char *pfx, char *s)
{
	for (;;) {
		if (!*pfx) return s;
		if (*pfx++ != *s++) return 0;
	}
}

static char *args_try_bool(struct arguments *ap, char *sp)
{
	for (; *sp;) {
		char *ep;
		if (*sp == 'y') ep = sp+1, ap->sync = 1;
		else if (*sp == 'r') ep = sp+1, ap->restart = 1;
		else if (*sp == 'O') ep = sp+1, ap->once = 1;
		else if ((ep = args_prefix("Cs", sp))) ap->use_srtp = 1;
		else if ((ep = args_prefix("CH", sp))) ap->high_pass_filter = 1;
		else break;
		sp = ep;
	}
	return sp;
}

#include <stdlib.h>

static enum args_result args_try_unsigned(struct arguments *ap, char *sp, char ***argv)
{
	unsigned *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if ((ep = args_prefix("-TiNGtrace", sp))) {
		t = &TiNGtrace;
	} else if (*sp == 'q') ep = sp+1, t = &ap->source_port;
	else if (*sp == 'p') ep = sp+1, t = &ap->dest_port;
	else if ((ep = args_prefix("oR", sp))) t = &ap->out_sample_rate;
	else if (*sp == 'n') ep = sp+1, t = &ap->numchan;
	else if (*sp == 'm') ep = sp+1, t = &ap->payloadmapping;
	else if (*sp == 'f') ep = sp+1, t = &ap->rfc2833tones;
	else if (*sp == 'a') ep = sp+1, t = &ap->agc;
	else if (*sp == 'X') ep = sp+1, t = &ap->maxoctets;
	else if ((ep = args_prefix("SSEt", sp))) t = &ap->sse_repeat_spacing;
	else if ((ep = args_prefix("SSEr", sp))) t = &ap->sse_repeat_count;
	else if ((ep = args_prefix("SSEp", sp))) t = &ap->sse_payload_type;
	else if (*sp == 'R') ep = sp+1, t = &ap->randdelay;
	else if (*sp == 'P') ep = sp+1, t = &ap->per_mod;
	else if ((ep = args_prefix("Cv", sp))) t = &ap->vad;
	else if ((ep = args_prefix("Ct", sp))) t = &ap->pticks;
	else if ((ep = args_prefix("Cp", sp))) t = &ap->clerk_port;
	else if ((ep = args_prefix("Cb", sp))) t = &ap->bitrate;
	else if ((ep = args_prefix("CV", sp))) t = &ap->variant;
	else if ((ep = args_prefix("CNm", sp))) t = &ap->comfort_noise_payload_mapping;
	else if ((ep = args_prefix("CL", sp))) t = &ap->frame_len;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtoul(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

#include <stdlib.h>
static enum args_result args_try_signed(struct arguments *ap, char *sp, char ***argv)
{
	signed *t;
	char *tp;// temporary only needed to avoid gcc warning
	char *ep;
	if (*sp == 'v') ep = sp+1, t = &ap->volume;
	else if (*sp == 's') ep = sp+1, t = &ap->speed;
	else if (*sp == 'T') ep = sp+1, t = &ap->threshold;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	tp = ep;// only needed to avoid 'may be undefined' gcc warning
	*t = strtol(tp, &ep, 0);
	if (*ep) {
		fprintf(stderr, "junk after value: '%s' for '%s'\n", ep, **argv);
		return ARGS_BAD_NOMSG;
	}
	return ARGS_OK;
}

static enum args_result args_try_string(struct arguments *ap, char *sp, char ***argv)
{
	char *ep, **t;
	if ((ep = args_prefix("-TiNGthreadtrace", sp))) {
		if (!*ep) ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
		TiNGtrace_file_perthread(ep);
		return ARGS_OK;
	} else if (*sp == 'x') ep = sp+1, t = &ap->identifier;
	else if (*sp == 'e') ep = sp+1, t = &ap->src_ip;
	else if (*sp == 'd') ep = sp+1, t = &ap->dest_ip;
	else if (*sp == 'c') ep = sp+1, t = &ap->codec;
	else if (*sp == 'K') ep = sp+1, t = &ap->srtp_key;
	else if (*sp == 'F') ep = sp+1, t = &ap->format;
	else if ((ep = args_prefix("Ca", sp))) t = &ap->clerk_addr;
	else return ARGS_NOMATCH;
	if (!*ep) {
		ep = *++*argv;
		if (!ep) return ARGS_BAD_NOARG;
	}
	*t = ep;
	return ARGS_OK;
}

static int args(struct arguments *ap, char ***argv)
{
	for (; **argv; ++*argv) {
		char *ep = **argv;
		if (*ep++ != '-' || !*ep) break;
		if (0) {
		} else if (!strcmp(ep, "-version")) {
			fprintf(stderr, "%s: version %s\n", progname, version);
		} else {
			int i;
			ep = args_try_bool(ap, ep);
			if (!*ep) continue;
			if (!strcmp(ep, "-")) {
				++*argv;
				return 0;
			}
			if (1
				 && !(i = args_try_unsigned(ap, ep, argv))
				 && !(i = args_try_signed(ap, ep, argv))
				 && !(i = args_try_string(ap, ep, argv))
			) {
				fprintf(stderr, "unrecognised option: %s\n", **argv);
				return 1;
			} else switch (i) {
				case ARGS_OK: break;
				case ARGS_BAD_NOMSG: return 1;
				case ARGS_BAD_NOARG:
				fprintf(stderr, "value missing for '%s'\n", **argv);
				return 1;
			}
		}
	}
	return 0;
}
